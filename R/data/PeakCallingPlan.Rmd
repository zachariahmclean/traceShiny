1. Peak table (like genemapper, bp size + height).
2. 1-3 example plots (maybe have a randomise button, next 1-3 button?) NOTE: This could be a module that could be used w/ ladder calling too.
3. Export/download all plots
